#!/bin/sh

set -e

case $1 in 

        configure)
		systemctl --global enable alumnatcleaner.service || true
                ;;
	   abort-upgrade|abort-remove|abort-deconfigure)
			   ;;
        *)
                echo "Nothing to do"
                ;; 
esac

#DEBHELPER#

exit 0

